#version 100

// fragment shader to create shadow map using depth texture
// esssentially normal rendering
// depth values are automatically written to texture bound to depth component in renderbuffer

precision highp float;

void main(void)
{
//    gl_FragColor = vec4(gl_FragCoord.z,gl_FragCoord.z,gl_FragCoord.z,gl_FragCoord.z);
}
